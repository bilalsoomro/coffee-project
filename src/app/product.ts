export class Product {
    width: number;
    height: number;
    length: number;

    constructor(width: number, height: number, length: number) {
        this.width = width;
        this.height = height;
        this.length = length;
    }
}